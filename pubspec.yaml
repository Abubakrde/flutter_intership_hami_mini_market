name: hami_mini_market_intership
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # Add these two lines for Week 3
  provider: ^6.0.0 # Or the latest stable version
  shared_preferences: ^2.0.0 # Or the latest stable version

  cupertino_icons: ^1.0.8

    

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/assets/logo.png
    
    